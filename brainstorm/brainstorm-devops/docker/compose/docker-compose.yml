version: '3'
services:
  database:
    image: brainstorm-db
    ports:
      - 5432:5432
    env_file:
      - ./secrets.env
    volumes:
      - brainstorm-db-data:/var/lib/postgresql/data
volumes:
  brainstorm-db-data:
